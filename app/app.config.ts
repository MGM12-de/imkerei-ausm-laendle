export default defineAppConfig({
  ui: {
    colors: {
      primary: 'honey',
      secondary: 'emerald',
      neutral: 'stone'
    },
    button: {
      slots: { base: 'font-semibold' }
    }
  }
})
